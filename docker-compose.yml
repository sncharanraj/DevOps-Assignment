version: "3.9"

services:
  backend:
    image: devops-backend:local
    ports:
      - "8000:8000"

  frontend:
    image: devops-frontend:local
    ports:
      - "3000:3000"
    environment:
      NEXT_PUBLIC_BACKEND_URL: http://backend:8000
    depends_on:
      - backend
